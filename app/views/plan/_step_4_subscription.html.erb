<div class="row">
  <div class="col-xs-12">
    <%= link_to(step_3_schedule_plan_path(role: @role, accreditations: @accreditations_ids), class: "link pull-left", remote: true) do %>
      <span class="glyphicon glyphicon-chevron-left"></span>
      <span class="hidden-xs hidden-sm">
        Cambiar acreditaciones
      </span>
    <% end %>
  </div>
  <div class="col-xs-12 text-center">
    <h3>Déjanos tus datos y te notificaremos cuando los cursos estén disponibles</h3>
  </div>
  <div class="col-xs-12 col-md-6 col-md-offset-3">
    <%# TODO: create a subscription %>
    <% @user = User.new %>
    <%= render "users/form" %>
  </div>
</div>
