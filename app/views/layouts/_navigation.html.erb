 <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("logo.png", class: "img-responsive"), "#", :class => "navbar-brand" %>
    </div>

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == "plan" %>">
          <%= link_to "Entrenamientos", root_path %>
        </li>
        <li>
          <%= link_to "Documentos", "#" %>
        </li>
        <li>
          <%= link_to "Presentaciones", "#" %>
        </li>
      </ul>
      <div class="divider"></div>
      <ul class="nav navbar-nav navbar-right">
        <% if admin_signed_in? %>
          <li class="<%= 'active' if params[:controller] == "courses" %>">
            <%= link_to "Cursos", courses_path %>
          </li>
          <li class="<%= 'active' if ["users","subscriptions"].include?(params[:controller]) %>">
            <%= link_to "People", users_path %>
          </li>
          <li class="<%= 'active' if ["accreditations"].include?(params[:controller]) %>">
            <%= link_to "Acreditaciones", accreditations_path %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle avatar" data-toggle="dropdown">
              <strong><%= current_admin.email %></strong>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to("Cambiar contraseña", edit_admin_registration_path) %>
              <li><%= link_to("Cerrar Sesión", destroy_admin_session_path, method: :delete) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
